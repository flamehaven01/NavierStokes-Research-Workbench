# v0.3.0 -- M5 scaling-atlas checkpoint

Release date: `2026-09-12`

Release status: `RELEASED[HOSTED_CI_VERIFIED]`

Version 0.3.0 expands the M5 code surface from one bounded `H_scaling`
vertical slice to a fixed `OutgoingDilation` scaling-atlas interface. It does
not claim a complete source-bound atlas, a Lean theorem proof, paper--Lean
semantic equivalence, a selected-source witness, or a solution of the
Navier--Stokes Millennium problem.

## Included checkpoint surface

- Adds selected-declaration batch normalization through one streaming raw
  export reader, without reading proof values or falling back to Lean source
  text.
- Defines ten reviewed-static scaling-law entries and exact `Fraction`
  exponent algebra. Degree-zero outputs are candidates for the next
  mathematical question, not invariants or estimates.
- Closes P1-A: candidate semantic digests cannot be supplied by the caller to
  production atlas admission. Admission requires a separately reviewed ledger
  file and a code-pinned physical SHA-256.
- Adds regression controls for source metadata drift, type-projection drift,
  raw-hash mismatch, law-metadata mutation, and recomputed attacker candidate
  digests.

## Evidence boundary

```text
P1-A digest authority              CLOSED[FAIL_CLOSED]
P1 static scaling algebra           PASS[LOCAL_REVIEWED_METADATA]
P1 exponent source derivation       NOT_AUTOMATICALLY_DERIVED_FROM_LEAN
P1 semantic digest authority        HELD[LEDGER_NOT_INDEPENDENTLY_PINNED]
P1 four-law same-raw export         ERROR[EXPORT_CAPACITY_TRUNCATED]
P1 ten-law source-bound atlas       HELD
```

The `289,992,704`-byte partial export is retained only as a failed execution
observation; it is neither committed nor used as source evidence. A complete
ten-law same-raw export, reviewed digest ledger, and code-pinned ledger hash
remain required before production atlas admission can pass.

## Hosted release evidence

Implementation commit `771232aab5d0537415ee33e674eba5f2c2c2fe64` passed the
complete pipeline in [GitHub Actions run 34683673649](https://github.com/flamehaven01/NavierStokes-Research-Workbench/actions/runs/34683673649):

```text
Python 3.10 / Ubuntu             PASS
Python 3.10 / Windows            PASS
Python 3.12 / Ubuntu             PASS
Python 3.12 / Windows            PASS
research validation and replay   PASS
pinned Lean targets consumed M4  PASS
```

The hosted Lean job remains scoped to the three M4 targets consumed by the
existing verifier. It does not export the four or ten M5 P1 declarations and
does not change their explicit held status above.
