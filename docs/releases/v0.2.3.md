# v0.2.3 -- Verifier evidence-boundary closure

Release date: `2026-09-12`

Release status: `READY[HOSTED_LIVE_VERIFIED__TAG_PENDING]`

Version 0.2.3 closes false-success and evidence-authority ambiguities found by
post-implementation review of the v0.2.2 checkpoint. It does not add a new
Navier--Stokes theorem, reconstruct the selected source witness, or establish
paper--Lean semantic equivalence.

## Highlights

- Makes M3 shell exit status agree with the recorded Lean PASS, FAIL, or ERROR.
- Derives hosted build classification from bounded GitHub Actions runtime
  identity and rejects receipt/provenance contradictions.
- Routes M4 manifests, Lean requests and receipts, migration records,
  provenance envelopes, and mutation metadata through strict JSON and their
  declared JSON Schema before runtime semantics.
- Adds a LIVE-only compiled-evidence v2 contract and independently re-hashes
  both `.olean` and dependency files in the same-run verifier.
- Makes Support boundary policy and Cone dependency ordering explicit data.
- Applies the 256-digit exact-rational limit independently to numerator and
  denominator.
- Commits 14 hash-bound pre-mutated JSON manifests and verifies that Python
  mutation generators reproduce them exactly.
- Separates implemented capability from future CFD, spectral, PINN, and
  neural-operator research directions in the public README.

## Local release-candidate verification

```text
pytest                         PASS[224]
coverage                       PASS[92.05% >= 90%]
ruff                           PASS
compileall                     PASS
git diff --check               PASS
v3 deterministic replay       PASS[BYTE_IDENTICAL]
v3 replay mutations           PASS[13/13_APPLICABLE]
v3 live-only mutation          NOT_APPLICABLE[LOCAL_REPLAY]
```

The deterministic replay SHA-256 is:

`027C241911F8957EF709CA0241216CE30E1DBB657CF9FACFA9648206E73842DF`

## Hosted release evidence

Release-candidate implementation commit
`30047196b2a4b9ca55b8364e7faa8c4363b0c39c` passed the complete pipeline in
[GitHub Actions run 34617896806](https://github.com/flamehaven01/NavierStokes-Research-Workbench/actions/runs/34617896806):

```text
cross-platform Python matrix       PASS[4/4]
research contracts and replay      PASS
pinned Lean same-run targets       PASS[3/3]
compiled evidence mode             LIVE_ARTIFACT
build cleanliness                  HOSTED_CLEAN_CHECKOUT
M4 live receipt                    PASS[COMPLETED]
applicable live mutations          PASS[14/14_REJECTED]
```

The downloaded evidence validates against the v2 compiled-evidence, v1
provenance, and v3 M4 schemas. Its canonical compiled receipt is bound to
GitHub Actions run `34617896806`, attempt `1`; the three uploaded dependency
files independently match their admitted SHA-256 values. See
`docs/stage_receipts/M4P_V0.2.3_HOSTED_LIVE.md` for identities and scope.

Historical run `34579077366` remains evidence for the untagged v0.2.2
checkpoint only.

## Research boundary

These checks establish only the stated software, source-binding, and compiled
target scope. M4-S remains `HELD[NONCOMPUTABLE_SOURCE_INSTANCE]`, paper--Lean
semantic equivalence remains `OPEN_RESEARCH_OBLIGATION`, and a solution to the
Navier--Stokes Millennium problem is `NOT_ESTABLISHED`.
