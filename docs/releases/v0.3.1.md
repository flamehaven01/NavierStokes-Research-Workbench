# v0.3.1 — P2 F1 compiled checkpoint

Release date: `2026-09-13`

Release status: `RELEASED[CHECKPOINT; LOCAL_COMPILED_P2_F1]`

Version 0.3.1 records one narrow compiled result in the active P2
dilation-ratio obstruction study and includes the next, intentionally
uncompiled L2-A scaffold. It is a research checkpoint, not a paper-level
verification, a full P2 formalization, or a Navier--Stokes result.

## Included compiled surface

At NSRW commit `5876e3bb27d48299cb963f5a8f809a8b29852302`, a clean local
Windows execution rebuilt the pinned `+NavierStokes.PulseAmplitude` target and
the external P2-F1 module. The recorded proposition is:

```text
0 < mainMoment c (1 : Fin 2)
```

The receipt records the source revision, toolchain, proof SHA-256, clean-tree
checks, exit codes, output hashes, and captured axiom surface:

- [`P2_F1_LOCAL_WINDOWS_2026-09-13.md`](../stage_receipts/P2_F1_LOCAL_WINDOWS_2026-09-13.md)

## Included but held surface

`formal/p2/P2_L2_NormalizedMainMomentComparison.lean` introduces only the
L2-A representation scaffold:

- normalized source-defined main moments;
- their short-log integral identity; and
- positivity of the normalized `i = 1` term by consumption of P2-F1.

It has no successful external-module compile receipt in this release. The
strict normalized comparison, `DeltaM < 0`, repair-coefficient signs, and
first-repair interior-zero consequence remain unpromoted.

## Evidence boundary

```text
P2-L1 pulse facts                         CONFIRMED[external compiled module]
P2-F1 mainMoment c 1 positivity           CONFIRMED[local compiled module]
P2-L2-A scaffold                           UNVERIFIED[external compile pending]
L2 strict comparison                       OPEN
DeltaM < 0                                 SUPPORTED / FORMALIZATION_OPEN
small-positive-eta first-repair zero       SUPPORTED / FORMALIZATION_OPEN
paper--Lean semantic equivalence           OPEN_RESEARCH_OBLIGATION
Navier--Stokes solution                    NOT_ESTABLISHED
```

The next execution task is a same-run external compilation of the exact L2-A
bytes with a freshly generated P2-F1 `.olean` dependency. A successful scaffold
compile would still not close L2; the strict comparison theorem is a separate
formal obligation.
